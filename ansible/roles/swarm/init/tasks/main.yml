- name: Swarm init
  block:
    - name: Init swarm
      shell: docker swarm init --advertise-addr {{ ansible_host }}
      args:
        creates: /var/lib/docker/swarm/state.json

    - name: Get manager join token
      shell: docker swarm join-token manager -q
      register: manager_token
      changed_when: false

    - name: Share manager join token with all managers
      set_fact:
        swarm_manager_token: "{{ manager_token.stdout }}"
      delegate_to: "{{ item }}"
      delegate_facts: true
      loop: "{{ groups['swarm_managers'] }}"

  rescue:
    - name: Leave swarm on failure
      shell: docker swarm leave --force
      ignore_errors: yes
