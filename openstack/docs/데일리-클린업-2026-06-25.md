# 데일리 클린업 — 2026-06-25 (목)

**작성:** 김현도

---

### 오늘 진행한 업무

- OpenStack **2노드 → 4노드 확장** (control + compute + network + storage 역할 분리)
- network(172.16.8.102, NAT×2) / storage(172.16.8.103, +100GB sdb) VM 신규 생성·초기 세팅
- control RAM **12→8GB 다이어트** + 전 노드 **Swap 4GB** (ansible 일괄 주입)
- storage Cinder 백엔드 구성 (`pvcreate /dev/sdb` → `vgcreate cinder-volumes`)
- `globals.yml` **Cinder 활성화** (`enable_cinder: yes`, `cinder_volume_group: cinder-volumes`)
- `destroy` → `bootstrap` → `deploy` 재배포 (4노드 전부 failed=0)
- post-deploy + Horizon + compute/network/**cinder-volume** 서비스 up 확인
- **00-common 완료**: 이미지(ubuntu-22.04), 플레이버(m1.small/medium), public1, project-public/private-net, project-router, project-sg, project_key
- **첫 tenant VM `swarm-mg-01`** 생성 (192.168.100.20, compute1, ACTIVE)
- Cinder 볼륨 생성+연결 검증 (test-vol → /dev/vdb)
- **Floating IP 172.16.8.219** 연결 → `ssh ubuntu@FIP` 접속 성공
- 문서 작성: `프로젝트-로드맵.md`, `문서화-마스터표.md` (§0~§9 1차 기록)

---

### 🚧 진행 중 겪은 문제점

- 102/103 root SSH: `ssh-copy-id` 거부 → root 비번 미설정 + `PermitRootLogin without-password`가 원인
  - 해결: `sudo passwd root` + `sshd_config.d/*.conf`의 PermitRootLogin을 yes로 교정
- `destroy` 시 102/103 `User root can't seem to run Docker` → 새 노드라 Docker 없음(무해), bootstrap으로 해결
- swap 100번 `Device or resource busy` → 어제 swapfile 이미 활성(무해), fstab만 보정
- (개념 정리) **VMware VM(4) ≠ tenant VM(11)** — tenant는 compute 안에 생성
- anti-affinity/분산은 **compute 1대라 아직 불가** → compute-02/03 추가 후로 보류

---

### 🎯 내일 진행할 업무 목표

- **호스트 RAM 확인** → compute-node-02 추가 가능 여부 판단
- compute-02 추가 시: **db01/db02 + db-server-group(anti-affinity)** 생성·배치 검증 (책임 10)
- 분산 무관 tenant VM 추가 (monitor-01, automation-01 등) — VM 목록 채우기 (책임 8)
- compute-03까지 가면 swarm-mg-02/03 분산 (책임 11)
- mgmt-01 VMware VM 추가 → "mgmt SSH" 산출물 (산출물 8)
- 마스터표 §1·§7·§8 갱신 (생성한 VM·server group·배치표)

---

### 책임 목록 진행도 (12개)

| 상태 | 항목 |
|------|------|
| ✅ (1~7,9) | 서비스, 이미지/플레이버, 네트워크, 서브넷, 라우터, SG, Floating IP, Cinder |
| 🟡 (8,12) | VM 생성 1/11, 문서화 1차 |
| ⬜ (10,11) | db anti-affinity, swarm 분산 (compute 추가 필요) |

**≈ 70%** (compute 1대로 가능한 건 거의 완료)

---

### 환경 스냅샷

| 항목 | 값 |
|------|-----|
| control / compute / network / storage | 172.16.8.100 / .101 / .102 / .103 |
| VIP / Horizon | 172.16.8.105 |
| swarm-mg-01 | 192.168.100.20 / FIP 172.16.8.219 |
| 외부망 | public1 (172.16.8.0/24) |
| tenant 네트워크 | project-public-net(100) / project-private-net(101) |
| Cinder VG | cinder-volumes (storage 103, ~100GB) |

---

## 한 줄 요약

> **2→4노드 확장 + Cinder 가동 + tenant 공통자원(네트워크/SG/키/이미지) 완성 + 첫 VM(swarm-mg-01) FIP SSH 성공 + 문서화. 책임 목록 약 70%, 남은 건 compute 추가 후 anti-affinity·분산.**
