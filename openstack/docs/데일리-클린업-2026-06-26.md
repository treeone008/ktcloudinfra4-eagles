# 데일리 클린업 — 2026-06-26 (금)

**작성:** 김현도

---

### 🗓️ 오늘 진행한 업무

- 오늘 나온 IP표 기준으로 **마스터표 전면 갱신** (compute2 `.104` / monitor `.110` / mgmt `.200` 신규, swarm-mg 연번, db_proxy `.40/.41`)
- `mgmt .200` IP 충돌 정리 → **Floating IP 풀을 `.201~.250`로 조정** (.200은 mgmt 전용)
- 4노드 클러스터 **실제 상태 점검**(`openstack ... list`) → 문서와 대조, **리소스 이름/ID 정리본** 작성(팀장 전달용)
- **보안그룹 `project-sg` 규칙 실측 → 설계와 100% 일치 확인**
- **anti-affinity 서버그룹 2개 생성**: `db-server-group`, `swarm-server-group`
- **compute2(.104) 제작 착수** — storage 끄고 그 RAM 자리에 compute2 올리는 전략(총 20GB 유지, 조정 불필요)
- compute2 = compute1 **Full Clone** → 식별자 정리(hostname·machine-id·SSH키·IP `.104`) → kolla **bootstrap ✅** → **deploy 67/68 태스크**
- 가이드 문서 작성: `02`(직접 합류), `03`(storage-off 로컬 합류)

---

### 🚧 진행 중 겪은 문제점

- **클론 식별자 충돌**: 클론이 compute1과 IP·hostname·machine-id·SSH키 동일 → 일일이 새로 발급해 해결
- **kolla `--limit` + 꺼진 storage 충돌**: bootstrap이 전체 노드 facts 수집하다 storage(.103) `No route to host`로 중단 → 인벤토리에서 `#172.16.8.103` **주석 처리**로 해결
- **compute2 nova-compute 등록 실패 (★ 미해결)**:
  - 컨테이너는 `Up(healthy)`인데 `compute service list`에 안 뜸
  - 로그: `MessagingTimeout: Timed out waiting for a reply`(conductor RPC) → 크래시 루프
  - 배제됨: RAM(여유 1.2Gi), RabbitMQ 알람(none), MAC 충돌(서로 다름), transport_url(정상), compute_id(삭제함), conductor(healthy·compute1은 정상)
  - 시도: compute_id 삭제 / rabbitmq·conductor·scheduler·api 재시작 / nova_compute 재시작 → 모두 실패
  - 잠정 결론: **클론 compute 특유의 nova RPC 등록 이슈** → 클린 재배포 또는 시연PC 릴레이로 마무리 예정

---

### 🎯 주말(내일) 진행할 업무 목표 — **compute2 + mgmt 완성**

1. **compute2(.104) 등록 마무리**
   - 방법 A: compute2의 nova/neutron **클론 잔재 컨테이너·볼륨 완전 제거 후 클린 재배포**
   - 방법 B: 안 되면 **시연PC 릴레이**(클론 아닌 깨끗한 노드로 합류)
   - 성공 시 → `scripts/create-db-antiaffinity.sh`로 **db01/db02 anti-affinity 검증**(책임 10 완료)
2. **mgmt(.200) 노드 신규 제작**
   - 외부 관리/SSH/Ansible 진입점 (지금은 control이 겸함)
   - bottom-up: Ubuntu + IP `.200` + SSH + (필요 시) ansible/cli 도구
   - "mgmt SSH" 산출물 정리
3. (여유 시) mgmt → control **Ansible 제어 구조** 초안 (cli/shell 실행파일로 인스턴스 생성 자동화)

---

### ⚠️ 원복 필요 (까먹지 말 것)

- `/etc/kolla/multinode`에 **storage가 `#172.16.8.103`로 주석 처리됨** → storage 다시 켤 때 **`#` 제거** 필수 (안 하면 cinder 누락)
- **storage 노드 꺼져 있음** (compute2 RAM 자리). 볼륨 작업 시 다시 켜기
- swarm-mg-01 현재 SHUTOFF

---

### 환경 스냅샷 (실측)

| 노드 | IP | 상태 |
|------|-----|------|
| control | 172.16.8.100 | ✅ up |
| compute1 | 172.16.8.101 | ✅ up |
| network | 172.16.8.102 | ✅ up |
| storage | 172.16.8.103 | ⏸️ 꺼둠(인벤토리 주석) |
| compute2 | 172.16.8.104 | 🟡 bootstrap/deploy OK, **nova 등록 실패** |
| VIP/Horizon | 172.16.8.105 | ✅ |

- 인스턴스: swarm-mg-01 (192.168.100.20 / FIP 172.16.8.219) — SHUTOFF
- 서버그룹: db-server-group / swarm-server-group (anti-affinity, 멤버 없음)
- 리소스 이름/ID: `docs/리소스-이름-ID-팀장전달.md` 참고

---

## 한 줄 요약

> **마스터표 오늘IP 전면갱신 + SG 실측검증 + anti-affinity 서버그룹 생성 + compute2 클론 제작(bootstrap·deploy까지). compute2 nova 등록만 conductor RPC 타임아웃으로 막힘. 주말에 compute2 클린 마무리 + mgmt 노드 제작 예정. (storage 꺼두고 인벤토리 주석 — 원복 필요)**
