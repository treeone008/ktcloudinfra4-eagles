# 데일리 클린업 — 2026-07-04 (토)

**작성:** 김현도  
**환경:** control / network / compute1 / compute-node-02 / **compute-node-03**

---

## 한 줄 요약

> **compute-node-03 합류** + tenant **10/11** 완료. swarm 3분산 ✅, proxy/lb 팀장 배치 ✅. monitor는 `.110`.

---

## 오늘 한 일

| 구분 | 내용 |
|------|------|
| VMware | **compute-node-03** (`.106`) 합류 |
| Kolla | bootstrap→pull→deploy, cell discover, storage `#` |
| tenant | lb×2, proxy×2, automation@03, **swarm-mg-03**, db02→03(m1.micro) |
| 문서 | `08` / 마스터표 / SSOT / 본 클린업 |

## 최종 배치

```
compute1          compute-node-02       compute-node-03
├─ swarm-mg-01    ├─ swarm-mg-02        ├─ swarm-mg-03
├─ lb-01          ├─ db01               ├─ db02
└─ db-proxy-01    ├─ lb-02              └─ automation-01
                  └─ db-proxy-02
```

## 남은 것 (선택)

- monitor-01 tenant — **불필요** (`.110`)
- db Cinder 볼륨 운영 연결
- swarm-mg-03 FIP (필요 시)
- 1차 마일스톤 zip (Shut Down 후 노드 폴더)

## 교훈

- `--limit` deploy 시 **꺼진 storage**는 inventory `#` 주석 필수
- VM 재생성 후 `ssh-keygen -R <IP>` (host key changed)
- m1.micro ACTIVE ≠ SSH 즉시 — cloud-init 대기
